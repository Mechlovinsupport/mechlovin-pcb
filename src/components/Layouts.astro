---
const { layoutSvg, name } = Astro.props;
---
<section class="section" id="layouts">
  <h2>Supported layouts</h2>
  {layoutSvg && (
    <div class="kle-stage">
      <img src={layoutSvg} alt={`${name} supported layouts`} class="layout-svg" />
    </div>
  )}
</section>
